


get_rb_path <- function (engine_path_from_options){
  # need to set up path to revbayes
  # base off of knitr:::get_engine_path(options$engine.path, 'rb')
  # typical use: rbPath <- RevKnitr::get_rb_path(options$engine.path)
  # 
  
  
  

}